# BhuVerify - SIH26018
# Intelligent Land Record Digitization and Validation System
#
# Prototype tier (P) dependencies.
# System dependency: tesseract-ocr >= 5.x  (+ language packs: eng, hin, ori)
#   sudo apt-get install -y tesseract-ocr tesseract-ocr-eng tesseract-ocr-hin tesseract-ocr-ori

# --- Web / API layer -------------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.38.0
python-multipart==0.0.20
pydantic==2.12.4

# --- Persistence -----------------------------------------------------------
SQLAlchemy==2.0.54
# Production tier swaps SQLite -> PostgreSQL + PostGIS (see SRS section 5.2)

# --- Computer Vision / OCR -------------------------------------------------
opencv-python-headless==4.11.0.86
numpy==2.3.5
Pillow==12.3.0
pytesseract==0.3.13
pypdfium2==5.7.0  # pure-python PDF renderer (Windows has no poppler-utils)

# --- NLP / fuzzy matching --------------------------------------------------
rapidfuzz==3.14.6

# --- GIS -------------------------------------------------------------------
shapely==2.1.2

# --- Security --------------------------------------------------------------
argon2-cffi==25.1.0

# --- Tooling ---------------------------------------------------------------
pytest==9.0.3
httpx==0.28.1
python-pptx==1.0.2
